schema:
  version: 4

command:
  program: python3
  args:
    - ../../common/dummy_hydra_app.py

merge:
  mode: merge

select:
  packs:
    - name: architecture
      priority: 0
      options:
        - id: mlp
          params:
            model:
              width: 256
              depth: 4
    - name: architecture_ablation
      priority: 10
      options:
        - id: width_512
          params:
            model:
              width: 512
