# 01 Conflict Behavior

Demonstrates priority-based conflict resolution in `merge.mode=merge`.

## Key Idea

When two packs set the same path, the higher-priority pack wins.

## Run

```bash
./launch.sh
```
