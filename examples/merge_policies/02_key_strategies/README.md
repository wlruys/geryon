# 02 Key Strategies

Demonstrates path-level merge strategies.

## Key Idea

Different paths can use different merge rules (`deep_merge`, `append_unique`, `set_union`, `replace`).

## Run

```bash
./launch.sh
```
